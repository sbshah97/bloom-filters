linters:
  enable:
    - gofmt
    - govet
    - revive
    - errcheck
    - staticcheck
    - unused
    - gosimple

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  deadline: 5m
  tests: true
